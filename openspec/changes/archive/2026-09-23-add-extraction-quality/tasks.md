# Tasks

## 1. 清洗与度量（纯函数）

- [x] 1.1 实现 `normalize_extracted_text(text) -> (清洗后文本, 清洗计数)`，覆盖三类改动：移除三个私用区（`U+E000–U+F8FF`、`U+F0000–U+FFFFD`、`U+100000–U+10FFFD`）；全角数字 `U+FF10–FF19`、全角大写拉丁 `U+FF21–FF3A`、全角小写拉丁 `U+FF41–FF5A` 各减 `0xFEE0` 转半角；折叠两侧均为 `U+4E00–U+9FFF` 的空格。计数按类别返回。验证方式：断言 `卫星<U+E5CE>的节点` 归一为 `卫星的节点`（不丢字）、`Ｔｅｌｓｔａｒ` 与 `１９６２` 归一为 `Telstar` 与 `1962`、`自 从二十世纪被提出 以来` 的词内空格被折叠而 `low orbit` / `a, b` 的空格保留
- [x] 1.2 实现 `assess_extraction_quality(text) -> dict`，返回私用区占比、全角拉丁占比、CJK 占比、表内字符占比、空壳表格率，以及基于前两项的判定结果。度量对象是**提取文本**，不是 chunk（design.md D11）。判定阈值以具名常量集中定义在一处（私用区 > 0.1%、全角拉丁 > 0.5%）。验证方式：对实测取值断言判定——封思敏 0.0408/0.1031 触发、其余六份 0.00/0.00 不触发；并断言空壳率取 0.713、CJK 占比取 0.0019 时**均不**触发
- [x] 1.3 断言清洗是幂等且确定的。验证方式：对含私用区与全角字符的样本断言 `normalize(normalize(x)) == normalize(x)`；对同一输入两次调用返回完全相同的文本与计数
- [x] 1.4 实现 `assess_coverage(chunks, text_len) -> dict`，返回**位置区间并集覆盖率**（并集 ÷ `text_len`）与**冗余**（Σchunk ÷ 并集 − 1）两个数，以及覆盖率是否低于阈值（99%，具名常量）。并集由 `start`/`end` 排序后合并得到。验证方式：断言重叠 chunk 使 Σchunk 超过 `text_len` 时覆盖率仍 ≤ 100%；断言冗余单独报出；对实测五份的取值（**口径为已存 chunk**，见 design.md D9：并集覆盖率 99.67%–100.00%、Σchunk÷文本 99.67%–114.72%）断言只有并集口径参与阈值判定；断言 `start`/`end` 缺失或区间不合法时不抛异常

## 2. 接入提取漏斗与知识库枚举

- [x] 2.1 在 `extract_markdown` 的「转换之后、写缓存之前」插入清洗与度量，并把 `_EXTRACT_CACHE` 的 value 由裸字符串改为记录（清洗后文本 + 清洗前度量 + 清洗后度量 + 清洗计数），缓存键保持 `(绝对路径, mtime, 大小)` 不变。验证方式：断言返回值是清洗后文本（不含私用区）、断言第二次提取命中缓存（`extraction_stats()` 的 `cache_hits` 递增且 `convert_calls` 不变）且拿到同一份记录
- [x] 2.2 新增按路径读取已缓存质量报告的入口，供 UI 与快速路径取用；未提取过的路径不抛异常。验证方式：对已提取的文件断言返回报告且判定与直接度量一致；对未提取的路径断言不抛异常
- [x] 2.3 断言位置契约未被破坏：chunk 的 `start` / `end` 指向清洗后文本的真实偏移。验证方式：对一份含私用区的样本**重新入库**后断言每个 chunk 满足 `markdown[c["start"]:c["end"]] == c["content"]`。注意断言只能对新入库数据做——存量 `default` 命名空间的偏移不成立（天地融合 19/19、冀博 2/2 实测错位），见 design.md Risks
- [x] 2.4 断言预览与入库仍然一致、指纹在缓存有无两种情形下都稳定。验证方式：同一文件预览后 `is_fresh` 为真；`clear_extraction_cache()` 之后再次断言 `is_fresh` 仍为真
- [x] 2.5 新增按命名空间枚举已入库文档的函数，返回 `(source_path, chunk 数)` 列表。实现为按 `rag_namespace` 分页 scroll 并按 `source_path` 聚合——`_document_point_ids` 必须传入 `file_path`，现有代码里没有这条路径（design.md D13）。验证方式：对 `docs_yyh` 断言列出 4 份 / 241 块（封思敏 87、唐斯琪 91、朱光熙 21、语义通信 42）、对 `default` 断言列出 4 份 / 100 块；断言结果不依赖助手实例的 `loaded_documents`
- [x] 2.6 实现从**已存 chunk 文本**度量某文档质量的函数，并先校验偏移：抽 chunk 比对 `content` 与 `markdown[start:end]`，吻合才认为位置可用。**必须同时比对清洗前与清洗后两份文本**——存量内容切自清洗前的原始提取，只比清洗后会把所有存量内容误判成不可用（实测封思敏 87/87、唐斯琪 91/91、朱光熙 21/21 会全部翻成 0，回看路径对整个知识库静默地不出覆盖率），且分母会错位（封思敏 172,500 ÷ 163,545 = 105.5%）。返回**匹配上的那份文本长度**供覆盖率做分母；两份都不吻合、或原文取不到，则标记位置不可用且不出这个数。验证方式：对一个构造的已入库样本断言返回度量且 `position_trustworthy` 为真；对天地融合与冀博断言为假；对封思敏/唐斯琪/朱光熙断言为真且 `text_len` 等于**清洗前**的长度

## 3. 告警与度量呈现

- [x] 3.1 `chunk_viz.render_chunk_report` 增加质量区块：判定不合格时显示噪声类别、各自占比、各类清洗计数，并**显式说明本次清洗不改变表格结构**；判定合格时不渲染该区块。验证方式：传入不合格报告断言 HTML 含告警节点、类别名与「表格结构」字样；传入合格报告断言不含告警节点。实测：封思敏 4.08/10.31 出区块，朱光熙 0.00/0.00 不出。注意判定要断言**节点**（`class="cv-quality"`）而不是字符串 `cv-quality`——后者也是 `_CSS` 里的样式名，每个报告都有，按字符串断言会永远通过
- [x] 3.2 在**预览的 stat 行**（`chunk_viz.render_stats`，`chunk_viz.py:131`）追加**一个** tile「内容覆盖率」，值取 1.4 的并集覆盖率，因此恒 ≤ 100%（使用者明确要求；Σchunk÷文本 的口径会读出 108.58%，被当成算错，故不采用）。冗余与两个原始字数放进该格的悬停提示，不另占一格。保留两位小数——一位小数会把 99.67% 与 100.00% 一起显示成「100.0%」。取数方式是 `render_stats(chunks, coverage=None)` 收 1.4 的返回值：`chunk_viz` 不 import `chunking`（它是零本地依赖的纯渲染模块），由调用方算好传入。`render_chunk_report` 透传 `coverage`；`workbench_preview` 传 `ck.assess_coverage(wb.chunks, len(wb.markdown))`；回看路径（`11_Q&A_Assistant.py:551/562`）读的是 Qdrant，拿不到提取文本长度，`coverage=None` 时**该 tile 不出现**。验证方式：断言 tile 值等于 `assess_coverage(...)["coverage_ratio"]`、恒 ≤ 100%、且与 Σchunk÷提取文本长度**不相等**（不相等才证明走的是并集口径）；断言 `coverage=None` 时 HTML 中不出现该 tile；断言回看路径的 HTML 与改动前一致。实测：封思敏 tile 100.00% vs Σchunk÷文本 108.84%；朱光熙 tile 99.92% vs 105.62%
- [x] 3.3 `preview_chunks` 的状态文本带一行告警摘要（噪声）与一行覆盖率摘要，合格且覆盖完整时保持现有文案不变。两行摘要由 `chunk_viz.summarize_quality` / `summarize_coverage` 产出——与面板区块出自同一份报告，措辞一致；覆盖率那行按 `below_threshold` 决定出不出现，所以「覆盖完整」= 不低于 99% 阈值。验证方式：分别用问题文档与健康文档预览，断言两个状态串一个含告警、一个与改动前逐字相同。实测：朱光熙的状态串逐字等于改动前（99.92% 未低于阈值）
- [x] 3.4 「🏠 开始使用」一键加载的返回串带同一摘要。原文写的是 `_index_with_strategy` 的「返回串」，但该函数返回的是 `(块数, 清理数)` 两个整数、没有返回串；字符串在它的调用方 `load_document` 里拼。故实现为：`_index_with_strategy` 改为返回 `(块数, 清理数, coverage)`（覆盖率在这层算，切分结果与提取文本都只在这里有；调用方只此一处），`load_document` 用同一对摘要函数拼消息。验证方式：一键加载问题文档断言结果串含告警；加载健康文档断言不含。实测走真实入库路径（写入后立即清空的临时命名空间 `docs_qualitycheck`，不动 `docs_yyh`）：
  ```
  加载成功！(耗时: 70.9秒)
  切分方式: Markdown 结构感知，共 83 个 chunk
  当前知识库包含 1 个文档
  ⚠️ 提取质量告警：私用区字符 4.08%（阈值 0.10%）；全角拉丁与全角数字 10.31%（阈值 0.50%）。本次已清洗：移除私用区 7,045 个、全角转半角 17,780 个、折叠汉字间空格 1,910 处；表格结构未修复。
  ```
- [x] 3.5 回看路径的告警文案要说明库中内容**尚未清洗**、重新入库才生效（design.md D12）。由 `render_quality_block(..., stored=True)` 产出这句。验证方式：对一份已入库且含噪声的文档调 `get_chunks_visualization`，断言 HTML 含告警且含「重新入库」字样；对同一份文档重新入库后再调，断言告警消失。实测（重新入库在临时命名空间进行，未动使用者知识库）：重新入库后 PUA 0.0000% / 全角 0.0000%、`position_trustworthy=True`、告警消失且覆盖率那格出现；同一份文档在 `docs_yyh` 里（未重新入库）仍告警
- [x] 3.6 「🔍 查看文档切分」的文档选择器（`11_Q&A_Assistant.py:1176`）改走 2.5 的枚举入口，不再用 `loaded_documents`。下拉框渲染不了表格，只能靠**选项标签**承载度量，故标签形如 `⚠️ PUA 4.1% 全角 10.5% 封思敏.pdf`，质量合格的文档只显示文件名（原名写的是「·」分隔，实现改成空格——标签本身已经很长，分隔符只会再挤掉文件名）。取值为 `source_path` 而不是文件名：`document_file_map` 随会话长出来、重启即空，按文件名取值会立刻变成「未找到文档」，回看必须能看上次会话入库的文档（D12 与 4.5）。验证方式：重启应用后不加载任何文档，断言下拉仍列出知识库中的全部文档；断言问题文档的标签含告警标记与两个占比，健康文档的标签不含。实测零加载状态下列出 4 份，标签 `⚠️ PUA 4.1% 全角 10.5% …`——**占比是非零的**（见下）。补充（实现后搜残留时发现）：写入同一个下拉框的还有第二条路径——workbench「✅ 入库」按钮的 `commit_preview`（`11_Q&A_Assistant.py:375`），它原先也用 `["当前文档"] + self.loaded_documents` 拼选项。只改 `refresh_doc_choices` 会让下拉框长什么样取决于你是怎么到这儿的：从「查看文档切分」进来带度量标签、取值是 `source_path`，从 workbench 入库一步就直接退回文件名，刚入库的那份反而看不到自己的度量。两条路径已统一走 `self.document_choices()`
- [x] 3.7 「📊 学习统计」的文档清单（`get_stats_ui` → `get_stats`，`11_Q&A_Assistant.py:857`）改为枚举入口，并以**一张 Markdown 表格**逐份呈现 chunk 数与质量度量。现在的做法是用正则从学习记忆里抠出文件名、再用换行拼进一个 bullet（`f"- **{key}**: {value}\n"`），多行会破坏列表结构——所以要改的是展示结构，不只是数据源。实现为 `documents_table()` 单独成表（列：文档 / Chunk 数 / 私用区 / 全角拉丁 / 空壳表格率 / 偏移校验 / 判定），`get_stats` 去掉「知识库中的所有文档」这一项、`_get_loaded_documents_from_memory` 因此无人调用，已删除。验证方式：断言输出含表头与每份文档一行；断言表内文档数等于 2.5 的枚举结果；断言 `_get_loaded_documents_from_memory` 不再被这条路径调用（已整个移除，故断言 `not hasattr`）。实测 4 行，`知识库文档总数 = 4`
- [x] 3.8 （实现中新发现，非原计划）`chunk_viz.quality_label` 必须吃两种形状的度量：`_quality_report` 的整份报告（占比在其 `before` 段里）与 `assess_extraction_quality` 的直接返回值（`assess_stored_document` 给的就是后者）。只按前者取 `report["before"]` 时，下拉标签静默显示 `PUA 0.0% 全角 0.0%`——不报错，只是把一份脏文档标成干净的。验证方式：断言标签里的数字与 `_stored_documents()` 同一份文档的 `pua_ratio`/`fullwidth_ratio` 逐字一致，而不是只断言标签含「PUA」和「%」（后者正是漏掉这个 bug 的断言写法）

## 4. 回归与端到端

- [x] 4.1 对已入库的 341 个 chunk 按清洗后文本重新度量一次，确认噪声归零而表格指标不变。（原记 299 个 / 7 份，写计划之后《大模型使能的语义通信研究现状与发展趋势》又多入库 42 块，故为 341 个 / 8 份。）验证方式：全集合扫描，断言封思敏清洗后私用区 0.00% / 全角拉丁 0.00%。实测 8 份 / 341 块，噪声逐份归零：
  ```
  %-30s %5s %17s %20s %14s %9s
  大模型使能的语义通信现状与发展趋势.pdf        42块  0.01%->0.00%  0.00%->0.00%  51.8%->51.8%  骨架 0/0
  朱光熙.pdf（多模态观测需求…）              21块  0.00%->0.00%  0.00%->0.00%  28.1%->28.1%  骨架 0/0
  天地融合网络智能组网体系架构研究.pdf          19块  0.00%->0.00%  0.00%->0.00%  60.7%->60.7%  骨架 0/0
  低轨巨型星座网络智能路由技术研究_封思敏.pdf     87块  4.12%->0.00% 10.46%->0.00%  51.8%->52.8%  骨架 6/31
  唐斯琪.pdf（智能化卫星互联网运维与管理）       91块  0.00%->0.00%  0.00%->0.00%  46.4%->46.4%  骨架 0/0
  （另 default 命名空间 4 份 100 块，全部 0.00%->0.00%）
  ```
  **原任务写的「空壳率仍约为 71%」两处都不对**，实测值 51.8%（存量 chunk 口径；那个 71% 来自写计划前另一次口径不同的探针），而且它**不**是不变的：封思敏 51.8% → 52.8%。原因是私用区字符正是 MarkItDown 填在表格单元格里的占位符——清掉之后 (a) 有些行末尾才露出 `|`，于是被识别成表格行（行数 +6），(b) 有些格原本「唯一的内容」就是占位符，清掉后成为空壳（空壳行 +31）。**方向是变差而不是变好**：清洗不修表格，只是让原本靠占位符撑着的空壳更显形。另外 7 份的骨架（行数、空壳行数）逐份不变。这条已同步进 design D5 与告警措辞（见 3.1 的「不修复表格结构」）
- [x] 4.2 端到端回归告警内容完整性。验证方式：对问题文档跑 `assistant.preview_chunks`，断言告警同时含噪声类别、占比、清洗计数与「表格结构未修复」四项；对健康文档跑同一入口，断言无告警。实测五项逐项为真（私用区 / 全角拉丁 / 占比 / `移除私用区 7,045 个`+`全角转半角 17,780 个`+`1,910 处` / `表格结构未修复`），面板区块另含「不修复表格结构」与两个表格指标；朱光熙的状态串与 HTML 都没有告警
- [x] 4.3 断言清洗的改动面**只有**这三类，不引入其它字符变化——这是本变更唯一能覆盖「清洗没改坏别的文档」的检查（汉字间空格会改动健康文档，故不能用「chunk 逐字相同」来断言）。验证方式：对全部 341 个 chunk（原写 299，见 4.1）逐个比对 `x` 与 `normalize(x)`。实测 341 块中 208 块发生改动，三类计数合计 私用区 7,788 / 全角 19,770 / 汉字间空格 3,130；每一块都等于独立实现的结果，长度只减不增（无插入），且全部幂等。实现这一检查时踩到两个坑，都记在这里：
  - **不要用 `difflib.SequenceMatcher` 做这个断言**：噪声文本的 opcode 极多，它是 O(n²)，341 块直接跑到超时（实测 120 秒未完成）。改用线性口径：独立实现一遍（删私用区 → 全角转半角 → 删两侧皆汉字的空格），逐字相等即证明没有别的改动
  - **独立实现必须与实现同样分两相**：先删私用区、再折空格。单相实现（用原文本的邻居判断空格）会不一致——`自 从` 的实现结果是 `自从`（私用区先被删掉，空格两侧于是成了两个汉字），单相实现会留下那个空格。初版断言错在这里，不是实现错
- [x] 4.4 覆盖率回归，并断言它不参与提取质量判定。验证方式：以**已存 chunk** 为口径（design.md D9）。实测四份可测文档逐格复现 D9 的表：
  ```
  封思敏  87块  覆盖率 100.00%  Σchunk÷文本 109.52%  冗余 +9.52%  判定 含噪声
  朱光熙  21块  覆盖率  99.94%  Σchunk÷文本 114.72%  冗余 +14.78% 判定 正常
  语义通信 42块  覆盖率  99.99%  Σchunk÷文本 108.58%  冗余 +8.60%  判定 正常
  唐斯琪  91块  覆盖率  99.67%  Σchunk÷文本  99.67%  冗余 +0.00%  判定 正常
  ```
  并集覆盖率全部落在 99.67%–100.00% 且恒 ≤ 100%；Σchunk÷文本 最高的是朱光熙 114.72%（>100%）而它**不**触发任何告警（冗余与噪声判定互不相干：封思敏冗余只有 9.52% 却因噪声告警）。可测文档是 4 份而非原写的 5 份——`default` 命名空间那 4 份（天地融合、冀博、Extra01、Hello-Agent）偏移全部不可信，按 D12 不出覆盖率。另注：断言不能写成「所有可测文档都不含噪声」，封思敏恰恰含噪声，这是本变更要告警的那一份
- [x] 4.5 回看路径对「本次会话未提取过」的文档仍可用（D12 的核心断言）。验证方式：重启进程后不清空任何缓存，直接对一份已入库文档调回看入口，断言仍给出质量度量；再断言天地融合这类偏移不可信的存量文档不呈现覆盖率。实测在进程内模拟重启（`clear_extraction_cache()` 清空提取缓存 + `document_file_map` 为空，即「本次会话既没提取过也没加载过」），只凭 `source_path` 回看封思敏：告警、`尚未清洗` 文案、覆盖率 100.00% 全都在。天地融合回看正常返回质量度量，只是没有覆盖率那一格。缓存清空后先断言 `cached_quality_report()` 返回 `None`，否则「未提取过」这个前提就不成立
