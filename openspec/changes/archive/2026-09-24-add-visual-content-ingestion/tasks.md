# Tasks

解释器一律用 `C:/Users/yyh15/.conda/envs/HelloAgent/python.exe`。本项目没有测试框架，
每条任务的验证都是一段一次性脚本加一组具体断言。写知识库的验证一律用
`docs_qualitycheck` 命名空间，事后清理并断言回到 0 块；`docs_yyh` 与 `default` 只读。

## 1. 动手前的准备

- [x] 1.1 确认 git 工作区干净，并记下当前 HEAD 作为回退点。
      验证：`git status --porcelain` 无输出；把 `git rev-parse HEAD` 的输出抄进本任务下。
      （本目录已是 git 仓库，回退靠 `git checkout`，不再另存 `*改造前.py` 副本。）
      **HEAD = `ad5c94e6796bdd4335b89d19ea04313d6415a2b0`**（分支 `main`）。
      实测说明：`git status --porcelain` 唯一一行是 `?? openspec/changes/add-visual-content-ingestion/`
      ——本次变更自己的规划产物，尚未提交。**已跟踪文件没有一处改动**，回退点成立。

- [x] 1.2 记录改动前的基线读数（**只读**，不写任何命名空间）：用 `ck.get_store()` 拿 store，
      实测并抄进本任务下——`docs_yyh`、`default`、`docs_qualitycheck` 各自的文档份数与块数，
      以及每份文档的 `source_path` 与块数。
      验证：脚本跑通并打印上表。**不要写死预期值**——知识库是活动数据，使用者可能已经换参数
      重新入库过；以本次实测为准，6.6 拿它比对。
      **已完成**（脚本 `%TEMP%\vision-verify\t1_2_baseline.py`）。实测读数：

      | 命名空间 | 份数 | 块数 | 逐份 |
      |---|---|---|---|
      | `docs_yyh` | 5 | 219 | 智能化卫星互联网运维与管理：现状与机遇_唐斯琪 91 / 大模型使能的语义通信研究现状与发展趋势 42 / 视频语义通信与编解码-理论学习笔记 40 / 人工智能物联网中面向智能任务的语义通信方法_NormalPdf 25 / 基于多模态观测需求信息的遥感星群任务智能规划机制_朱光熙 21 |
      | `default` | 4 | 100 | Extra01-参考答案 78 / 天地融合网络智能组网体系架构研究_NormalPdf 19 / 冀博-从SDD到确定性质量门禁 2 / Hello-Agent 1 |
      | `docs_qualitycheck` | 0 | 0 | ——（本次的试验田，应为空） |

      **与规划时的读数又不同了**：`add-chunking-workbench` 记的是 `docs_yyh` 5 份 / 266 块，
      且含封思敏 87 块。现在 `docs_yyh` 是 219 块、**封思敏已不在其中**，而 5 份里多出两份
      语义通信相关的文档。这再次印证「基线会过期，验证前先取实测值」（见 6.6 的处理）。
      附带确认了归档任务里那条提醒：裸脚本里 `RAGTool(...)` 不走 `.env` 的 LLM 配置，
      会打印「❌ RAG工具初始化失败: API密钥…」，但 **store 仍然可用**——本脚本与 6.x 不受影响。
      （后来把这条的根因查实了：那些脚本写的是 `load_dotenv(override=True)`——**没给路径**，
      dotenv 从当前目录往上找，而脚本是从 `%TEMP%` 里跑的，那棵树里没有 `.env`。于是
      `LLM_API_KEY`/`LLM_BASE_URL` 没进环境，`hello_agents.core.llm:83` 抛异常，
      `rag_tool.py:87-89` 接住它、`initialized=False`、打印那一行；而此前已建好的 pipeline
      仍在 `self._pipelines` 里，所以 store 照用。缺的是**问答用的 LLM** 那一套凭据，
      与 `VLM_*`（视觉）和 `EMBED_*`（嵌入）无关。6.1/6.4/6.5 显式传了
      `os.path.join(PROJECT, ".env")`，所以没有这一行。顺带一提，
      `11_Q&A_Assistant.py:14` 那句 `load_dotenv(override=True)` 同样没给路径，
      它只在「应用从项目目录启动」时有效——6.5 的脚本 bug 正是被这一点坑的。）

- [x] 1.3 记录改动前封思敏 PDF 与 `测试上传图片.png` 的提取指纹，作为「视觉未启用时行为不变」
      的回归基线：对两份文件各跑一次 `ck.extract_markdown()`，抄下字符数与 md5。
      验证：断言封思敏那份的换页符 `\f` 计数为 0、HTML 注释计数为 0（design.md Context 1
      的前提）。图片那份应当是一串 PNG 块标识（`IHDR`/`IDAT`）——把它抄下来，
      它是 D10 要消掉的那个行为。
      **已完成**（脚本 `%TEMP%\vision-verify\t1_3_baseline_extract.py`）。实测读数：

      | 素材 | 源路径 | 字符数 | md5 | `\f` | `<!--` | 含 IHDR/IDAT |
      |---|---|---|---|---|---|---|
      | 封思敏 PDF | `C:\Users\yyh15\Desktop\Patent\Agent\低轨巨型星座网络智能路由技术研究_封思敏.pdf` | 163545 | `35b7080ded54c3a0bcf3af01e57cdb7f` | 0 | 0 | 否 |
      | 测试上传图片.png | `C:\Users\yyh15\Desktop\测试知识库\测试上传图片.png` | 89058 | `902821766792dace3a3d19eb8c391d51` | 576 | 0 | **是** |

      两条断言都成立：PDF 的页界确实是 0（design.md Context 1 的前提在本次素材上复现），
      PNG 的提取结果开头就是 `'PNG\n\x1a\n\x00\x00\x00\nIHDR\x00\x00\x04v...pHYs...tEXtSoftware\x00Snipaste...IDATxK#m\x0c\x17_'`
      ——89,058 字符的二进制噪声，却带着 100% 的覆盖率（t1.3 之前那次工作台预览是 95,741 字符、
      100.00%，差异来自当时的切分参数而非提取）。这正是 D10 要消掉的那个行为。

      **注意素材位置**：`%TEMP%\gradio\<hash>\` 下那份副本已被清理，两份源文件改用上面的
      桌面路径。6.5 回归比对用的是本表的字符数与 md5。

## 2. `vision.py`

- [x] 2.1 建 `vision.py`，实现「文件 -> 每页的渲染图」。PDF 用 `pypdfium2` 逐页渲染
      （scale 1.5），图片文件按单页处理。
      验证：对封思敏 PDF 跑一次，断言返回的页数与 `pypdfium2` 独立读到的页数相同，
      且每页位图尺寸为 819×1243（A4 在 scale 1.5 下）。`vision.py` 内
      `grep -n "^from 11\|^import 11"` 无输出（不 import 主程序）。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_1_render.py`）。实测：
      封思敏 PDF **70 页**，`render_pages` 返回 70 页 == `pypdfium2` 独立读到的 70 页；
      70 页尺寸**去重后只有一个值 `(819, 1243)`**（断言里的 819×1243 成立，且不是平均值
      ——是每页都如此）；页码序列为 1..70；每页 PNG 魔数 `\x89PNG\r\n\x1a\n` 正确。
      渲染 70 页耗时 **4.7 s**（一次性的本机成本，相对云端 19 min 可忽略）。
      第 31 页：819×1243，PNG 404807 B（≈395 KB，base64 后约 527 KB/请求），
      文本层 1439 字符。图片文件 `测试上传图片.png` 按单页处理：1142×759、159296 B、
      文本层 0 字符（独立图片没有文本层，符合 D9）。
      `grep -n "^from 11\|^import 11" vision.py` 无输出。

- [x] 2.2 实现视觉配置读取与配置指纹（design.md D4）：读 `.env` 的 `VLM_BASE_URL` /
      `VLM_MODEL_ID` / `VLM_API_KEY`，指纹 = base_url + 模型标识 + 提示词哈希 + scale。
      验证：断言三件事——只改 `VLM_MODEL_ID` 时指纹变化；四项都不动时指纹不变；
      三项里缺任一项时返回「未配置」。**断言输出里只出现变量名，不出现任何 `*_KEY` 的值**
      （禁区 7）。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_2_config.py`）。实测：
      当前配置 `model=qwen3-vl-235b-a22b-instruct`、`base_url=https://aihubmix.com/v1`、
      scale=1.5，**配置指纹 = `715ebba3349fda0c`**（2.6 要用它）。
      四项**逐项**都验证了会改变指纹：模型 → `434cd47b7d9e08b0`；base_url →
      `dc6ca82c0a4b0264`；scale → `5486890e20dab13d`；**提示词 → `6c57e1d4343f0f34`**
      （这一项是 D4 里最容易漏的：漏了就会出现「改了提示词却继续复用旧转写」）。
      三项分别置空都读出「未配置」，`missing_config_names()` 精确指出缺的是哪一个；
      全缺时 `extract()` 抛 `VisionUnavailableError` 且消息里含缺失项名——3.4 要复用这条。
      密钥检查：`describe()` 与指纹里都不含任何密钥（比对了 7 个密钥候选值），
      且 `VLM_API_KEY` 不出现在 `VLM_BASE_URL` 里（否则 `describe()` 会把密钥写进
      `meta.json`）。`describe()` 的实际内容只含 base_url / model / scale / prompt_sha256
      / fingerprint。

      **两个踩到的坑，记下来免得重犯**：
      1. 验证脚本放在 `%TEMP%` 下，`load_dotenv()` 无参会从**脚本所在目录**向上找 `.env`，
       找不到项目里那份，于是三项全缺。必须写成 `load_dotenv(<项目>/.env, override=True)`。
       （1.2 / 1.3 的脚本也有同样写法，但它们的读数不依赖 `.env`，结论不受影响。）
      2. `fingerprint` 是 **property**，每次访问都按当前 `PROMPT` 重算。测试「改提示词 → 指纹变」
       时必须**先**把旧指纹存进变量再改，否则两边都用新提示词算，断言恒等于失败。

- [x] 2.3 实现单页调用：渲染图 -> 文本。提示词要求输出 markdown 结构（标题层级、
      题注原样保留），并要求把图中可读信息写成文字、无法精确辨识的数值如实说明。
      验证：对封思敏第 31 页的渲染图调用一次，断言返回非空、且文本含「2.2.2 算法实现」。
      把实际耗时抄进本任务下。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_3_single_page.py`，样本留在同目录
      `t2_3_page31.txt`）。实测两次，均一次成功（Python 侧重试次数=1）：
      **耗时 25.5 s、34.0 s**（同一页 819×1243、同一个模型、同一台机器）。
      2595 字符级别的返回，断言全部成立：含 `2.2.2` 与 `算法实现`；正文、公式
      （`\quad \text{(2-28)}`）与图注都转写下来了。

      **本任务暴露了一个真问题，已修**：第一次跑时模型写的标记是

          [[VISION 图 2-5 MLRA 算法框架图      <- 开标记本行没有 ]]
          该图为框图，……
          ]]                                     <- 闭标记写成单独一行的 ]]
          图 2-5 MLRA 算法框架图                  <- 又把题注重复了一遍

      与提示词要求的成对形态不一致。当时的 `normalize_vision_markers` 认不出它，
      `count_vision_blocks` 对**明明有图的第 31 页报了 0**——D6 的保护区间会在真实输出上
      静默失效，视觉描述会被切分器切开而**不报任何错**。这正是设计里最怕的一类失败。
      两处一起改：
      1. `normalize_vision_markers` 改为容忍全部观察到的变体（开标记缺 `]]`、闭标记写成
         单独的 `]]`、同行收尾、漏闭合、无主闭标记），一律收敛成规范形态。下游只认一种。
      2. 提示词收紧一句，明确「开标记与题注同行并以 `]]` 结尾、闭标记单独占一行」。
      改后重跑，模型直接给出规范形态，规整后**视觉块 = 1**，块内含题注「图 2-5」。
      变体的单元验证见 `t2_3b_markers.py`（10 个用例全过，不花 API 钱），其中
      第 1 个用例就是上面这段**实测原文**。
      代价：提示词进了指纹，所以本次改动把配置指纹从 `715ebba3349fda0c` 换成了
      **`dae5a1d55f945964`**——旧缓存会自动失效，这正是 D4 想要的。

- [x] 2.4 实现重试与指数退避，重试发生在**页**这一层而不是整份文档那层（design.md D11）。
      验证：注入一个「前两次抛 `URLError`、第三次成功」的假客户端，断言总尝试 3 次、
      最终成功、且相邻两次的等待时长递增。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_4_retry.py`，全部注入假客户端，不花钱）。
      实测全部成立：前两次 `URLError`、第三次成功 → 尝试 3 次、等待 `[2.0, 4.0]`（严格递增，
      且等于 `RETRY_BASE_DELAY × 2^(n-1)`）；一次成功 → 尝试 1 次、**不等待**；
      全部失败 → 调用 4 次（=`RETRY_ATTEMPTS`）、等待 3 次（最后一次失败后不再等待）、
      抛出 `URLError`。另外补了一条 D11 的要害断言：三页里只有第 2 页首次失败时，
      各页调用次数为 `{1: 1, 2: 2, 3: 1}` —— **重试确实只发生在失败的那一页**，
      不是整份文档重跑。
      附带：`extract()` 要按路径算文件指纹，所以喂给它的必须是真实存在的文件；
      验证脚本里用临时文件，跑完删掉临时文件与其缓存目录。

- [x] 2.5 实现逐页落盘缓存（design.md D4）：每页一个 JSON，`meta.json` 记文件指纹与
      **完整的**配置指纹四项。
      验证：对同一份文件连跑两次，断言第二次对模型的调用次数为 0，且两次产出的文本逐字相同。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_5_cache.py`，假客户端计数，不花钱）。
      实测：第一次 70 页 / 调用 **70** 次 / 缓存命中 0；第二次 70 页 / 调用 **0** 次 /
      缓存命中 **70/70**；两次产出 **逐字相同**（3611 字符）。
      落盘物：`70 个 page-*.json + meta.json`，`meta.json` 里 `file.fingerprint` 与
      `config.fingerprint` 都对得上，`config` 四项齐全（base_url / model / scale /
      prompt_sha256）——**注意 `prompt_sha256` 现在是 `711bc400a8420f9a`**（提示词在 2.3
      收紧过，与 2.2 记的不是同一个提示词）。逐页状态计数 `{'ok': 70, 'empty': 0,
      'failed': 0}`，摘要串形如 `视觉解析：70 页 —— 70 成功`。
      禁区 7 复查：落盘缓存的所有文件里都不含 `VLM_API_KEY` 的值。
      **验证脚本用的是 PDF 的临时副本**，不是原件：脚本写进去的是假转写，
      万一清理失败也不会让 6.1 的真实入库读到假文本。跑完临时副本与其缓存目录都已删除。

- [x] 2.6 换配置不复用旧产物。
      验证：跑完一次后改 `VLM_MODEL_ID` 再跑，断言模型调用次数大于 0，且新文本与旧文本不同；
      `meta.json` 里两项配置指纹都可读出。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_6_reconfigure.py`）。实测：配置一
      `dae5a1d55f945964` 跑满 70 次调用；改 `VLM_MODEL_ID` 后指纹变
      `67dce6a30c6bc40c`，**重新调用 70 次、缓存命中 0**（若为 0 就说明复用了旧产物），
      新旧文本不同；`meta.json` 里 `file.fingerprint`（未变）与 `config.fingerprint`
      （=新配置）两项都能读出。

      **本任务改了一处落盘布局**：逐页文件原先是
      `.chunk_cache/<文件指纹>/page-NNNN.json`，两套配置平铺在同一个目录里、只靠 JSON 内
      的 `config_fingerprint` 判别。实测发现这样**换模型重跑会覆盖上一次的产物**——换回原模型
      要重付 70 次调用（约 20 分钟的真实花费）。而「比较哪个模型读得准」正是把模型做成可配置
      的原因，这个来回恰恰是最该省下的，不是边缘场景。改为**多一层配置指纹**：

          .chunk_cache/<文件指纹>/<配置指纹>/page-NNNN.json     （meta.json 留在上一层）

      `meta.json` 仍在文件指纹那一层，记最近一次运行用的配置，位置与含义都没变。
      改后补了两条断言：**两套配置各读回自己的文本、互不串味**（旧读到 `[A|…]`、新读到
      `[B|…]`），以及**换回原模型时调用 0 次、文本与最初逐字相同**。
      这是 D4「缓存以四项指纹为键」的直接推论，已补记进 design.md D4。

- [x] 2.7 中断后可续跑。
      验证：跑到第 10 页时终止进程，重新跑同一文件，断言只对第 11 页及之后调用模型
      （前 10 页取自落盘结果）；最终文本与一次跑完的文本逐字相同。
      **已完成**（`%TEMP%\vision-verify\t2_7_resume.py` + 子进程 `t2_7_child.py`）。
      子进程用 `os._exit(1)` **真的把进程杀掉**（不走清理），退出码 1，杀掉时已落盘 10 页。
      续跑实测：模型调用 **60** 次、页码 **11..70**、缓存命中 **10** 页。
      为了让「谁写的」在文本里看得见，子进程与父进程用了不同 tag：
      **前 10 页仍是 `[CHILD]` 写的、第 11 页起是 `[PARENT]` 写的**——这才真正证明了
      前 10 页取自落盘结果而不是被重算。
      另做一份「一次跑完 vs 中断后续跑」的对照（同样 tag）：调用 70 次 vs 60 次，
      两份文本 **逐字相同**（1039 字符），没有丢页或重页。

- [x] 2.8 实现逐页文本层读取，供「同页对照」与「失败页回退」使用（design.md D9）。
      验证：断言返回的页数与渲染页数相同，且第 31 页的文本层里能找到 `算法实现` 这样的
      实际字符串（哪怕是拆碎的形态）。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_8_layer.py`，不调用模型）。实测：
      70 页全部有文本层（**0 页为空**），合计 **96946** 字符，两次渲染逐页逐字相同
      （md5 `a4d8155aded9`）——失败页回退因此是可复现的。
      第 31 页 1439 字符，**去掉空白后**含「算法实现」✓。

      **实测到一件必须记下来的事：文本层是「拆碎的」，而且比预想的更糟。** 原始形态是

          北 京 邮 电 大 学硕 士 学 位 论文
          ２ ． ２ ． ２ 算法 实现
          深 度 强化学 习 包含价值 导 向 与 策 略 导 向 两大 方法论 。

      两处损伤：① PDF 把每个字单独定位，提取出来字与字之间插了空格——第 31 页
      **596 个 CJK 字里有 212 处相邻字对被插了空格，占 35.6%**；② 小节号是全角
      `２．２．２`。任务原文说的「哪怕是拆碎的形态」，实测就是字面意思。
      影响面：这只影响 **D8 的失败页回退**（视觉成功时根本不读文本层），而且状态串已经
      明说「这些页的正文取自既有文本层，未经视觉处理」，不是静默降级（禁区 9）。
      但它意味着**一旦某页失败，那一页的可检索性明显劣于视觉转写**——所以失败要修，
      而不是指望回退能顶住。本次不做任何清洗：清洗属于既有漏斗（3.x 会统一走
      `normalize_extracted_text`），在这里另加一套启发式清洗正是禁区 4/9 要避免的。

      顺带回答了 design.md Open Question 2 的一半：pypdfium2 逐页文本层 96946 字符 vs
      库提取 163545 字符（比值 0.593）。**这个比值不能当质量指标**——库那份含大量
      markdown 表格骨架，字符数被撑大。按 D7，只呈现，不判定。

- [x] 2.9 实测单页超时的取值（design.md Open Questions 1）：对若干页分别测耗时，据实测定超时值，
      把测得的分布与取值理由抄进本任务下。
      验证：脚本打印每页耗时与最大值；取值必须大于实测最大值并留出重试预算。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_9_timeout.py`，6 次真实调用）。采样页
      `[1, 15, 31, 45, 58, 70]`，全部成功：

      | 页 | 耗时 | 尝试 | 转写字符 | 文本层字符 | PNG |
      |---|---|---|---|---|---|
      | 1 | 14.7 s | **2** | 321 | 218 | 740 KB |
      | 15 | 33.5 s | 1 | 1394 | 2273 | 452 KB |
      | 31 | 23.4 s | 1 | 1239 | 1439 | 395 KB |
      | 45 | 24.2 s | 1 | 990 | 1111 | 289 KB |
      | 58 | **60.8 s** | 1 | 879 | 772 | 180 KB |
      | 70 | 9.8 s | 1 | 862 | 1059 | 115 KB |

      最短 9.8 s / 中位 23.8 s / **最长 60.8 s** / 平均 27.7 s。

      **取值：`PAGE_TIMEOUT = 180.0` 秒**（实测最长的 3.0 倍）。理由：①页间方差有 6 倍
      （60.8 vs 9.8），固定倍数宁可宽一点也不宜卡边；②两侧代价不对称——定低了会把本来
      能成的页判成失败，那一页就只剩空格拆碎的文本层兜底，定高了只是在真正挂起时多等
      几分钟，且每页一次运行里最多发生一次。单页最坏总耗时（含重试）= 180×4 = 12 分钟。

      **两条顺带发现，都比预期差**：
      1. **第 1 页尝试了 2 次**——真实运行里确实出现了首次失败，重试机制不是纸上谈兵。
         （首次失败很快，不是超时，是通路抖动；与 design.md 记的 `ssl.SSLEOFError`
         一类现象吻合。）
      2. **实际比 design.md 的估算慢不少**：D3 记的是 235B 云端 16.5–18.1 s/页，本次样本
         平均 **27.7 s/页**、中位 23.8 s/页。按此推算 70 页约 **28–32 分钟**，而不是 D3
         写的约 19 分钟。这不是设计错误（当时的样本不同、通路负载也会变），但使用者
         安排时间时应当按 30 分钟量级预期，而不是 20 分钟。

- [x] 2.10 实现逐页状态（`ok` / `empty` / `failed`）与逐页字符计数。
      验证：构造三种结局各一页（假客户端分别返回正常文本、空串、抛异常），
      断言状态与字符数三者分别正确，且 `failed` 页不被计为成功。
      **已完成**（脚本 `%TEMP%\vision-verify\t2_10_status.py`，假客户端）。实测三页
      分别落到三种结局：第 1 页 `ok`/12 字符；第 2 页 `empty`/0 字符；第 3 页 `failed`、
      正文**确实等于该页文本层**、异常记在 `error` 字段。
      计数 `{'ok': 1, 'empty': 1, 'failed': 1}`，**`ok` 计数没有把 failed 页算进去**；
      `failed_pages()` / `empty_pages()` / `fallback_pages()` 都正确。
      逐页字符数可读：`[(1,'ok',12,8), (2,'empty',0,8), (3,'failed',11,11)]`。
      状态串（5.x 要接进预览的就是它）：

          视觉解析：3 页 —— 1 成功 / 1 空 / 1 失败
            ⚠️ 未成功：第 3 页；这些页的正文取自既有文本层，未经视觉处理

      **一条刻意设计的行为，在这里被验证了**：`failed` 页**不落盘**，`ok`/`empty` 落盘。
      实测落盘文件只有 `page-0001.json`、`page-0002.json`。于是再跑一次时**只重新调用
      第 3 页**（调用记录 `[3]`、缓存命中 2），失败页转为 `ok`，计数变成
      `{'ok': 2, 'empty': 1, 'failed': 0}`。而 `empty` 之所以落盘，是因为它是
      temperature=0 下的稳定回答，重复调用既贵又没有意义。

## 3. 接入提取漏斗

- [x] 3.1 在 `_extract_record()`（`chunking.py:140`）内插入视觉段：视觉可用且文件为 PDF 时，
      T′ 由逐页「转写 + 视觉块」拼成；此时不再调用 `_convert_via_library()`（design.md D9）。
      视觉块按 D6 的成对标记写成自成一段。
      验证：断言 `ck.extract_markdown(封思敏.pdf)` 的结果含 `[[VISION` 与 `[[/VISION]]`，
      且字符数与 1.3 记的基线不同。
      **已完成**（脚本 `%TEMP%\vision-verify\t3_1_3_3_real.py`，真实调用模型）。实测读数：

      ```
      模型 qwen3-vl-235b-a22b-instruct  scale=1.5  timeout=180.0s
      配置指纹 dae5a1d55f945964

      耗时 20.1 分钟（1205.5s / 70 页 = 17.2 s/页）
      实际调用模型 45 次，命中磁盘缓存 25 页
      状态计数 {'ok': 70, 'empty': 0, 'failed': 0}
      T′ = 78359 字符，md5 059b22ebda857d59ab906fcfd2df1b3a

      [✓] 3.1 T′ 含 `[[VISION` ×26、`[[/VISION]]` ×26
      [✓] 3.1 T′ 字符数 78359 != 基线 163545（差 -85186）
      [✓] 3.1 无失败页（failed = 0）

      有视觉块的页 25/70，视觉块总数 26
      ```

      三条断言全过：26 对标记**成对**（`[[VISION` 与 `[[/VISION]]` 各 26 个）、
      70 页全部 `ok`、没有失败页。这次运行的部分页来自磁盘缓存（45 次真实调用 +
      25 页缓存），所以耗时比 2.9 估的 28–32 分钟短——分母换成真实调用是
      **1205.5s / 45 = 26.8 s/次**，与 2.9 实测的 27.7 s 吻合。

      **这次实际上并发跑了两份，把缓存跑成了竞态**（操作失误，如实记下）。
      后台还留着上一个进程没停掉，两份同时抓同一批缺页、写同一个缓存目录，两份都
      跑完了、都 PASS 了，但报出的数不一样：

      | 进程 | 耗时 | 真实调用 | 缓存命中 | T′ | 视觉块 |
      |---|---|---|---|---|---|
      | `b44r81xx9` | 24.7 分钟 | 68 次 | 2 页 | 78691 字符 / `ec3f95a423b1fdc1a4dcc187e82f0f31` | 27（26 页） |
      | `bav0xm1cj`（上表） | 20.1 分钟 | 45 次 | 25 页 | 78359 字符 / `059b22ebda857d59ab906fcfd2df1b3a` | 26（25 页） |

      缓存里的落盘文件按页看时间戳能证明并发——**第 29 页写在 28 页之前、24 页写在
      23 页之前、63 页写在 61 页之前**，单进程的顺序循环不可能这样。最后写进去的是哪一份
      取决于谁后写，于是有 6 页（31/35/36/59/60/61）落盘的是另一个进程的文本：
      上表里它们的转写是 1222/794/1187/1036/993/1097，而**以缓存为准重读是
      1277/867/1240/1027/1004/1071**。

      **以缓存为准的实测值（后续 6.1–6.5 看到的就是这个）**：

      ```
      T′ = 78568 字符，md5 9286efb26939b6b0e96c1540c4a53b30
      逐页转写字符合计 78454（+ 69 个页间分隔符 ×2 = 78592，清洗后 78568）
      70 页 ok / 0 空 / 0 失败，视觉块 26 个，分布在 25 页上
      ```

      两件事值得留在案上：①**落盘缓存没有加锁**，两个进程并发跑会互相覆盖，
      「T′ 是多少」在多写者下不是单值——单用户命令行场景不必修，但复跑验证时
      只许有一个写者；②**这个模型在 `temperature=0` 下并不确定**，同一页两次调用
      文本长度能差 73 字符、视觉块数 26 vs 27。所以缓存不只是省钱的手段，
      它是「同一份文件得到同一份 T′」的唯一保证——`vision.py` 里那句
      「空结果是 temperature=0 下的稳定回答」的理由已按实测改掉。

      **T′ 比基线短了约 85000 字符**（78568 vs 163545）。这不是「丢了内容」，而是
      D9 的缝合方式决定的：视觉可用时**不再调用** `_convert_via_library()`，正文
      整份换成模型转写。逐页看，转写与文本层各有长短（下表取自 `b44r81xx9` 那次，
      它 68 页都是实时调用、受并发影响最小）：

      | | 字符数 |
      |---|---|
      | 视觉转写（70 页之和） | 78245 |
      | 文本层（70 页之和） | 96946 |
      | 其中**无视觉块**的 45 页文本层 | 68077 |

      （78245 是逐页 `p.chars` 之和；T′ 比它多出的是页间分隔符减去清洗掉的部分。）

      也就是说：**每一页的正文都来自转写**（视觉可用时库提取整条不参与），没产出视觉块
      的那 45 页，其文本层的 68077 字符就没进 T′；产出视觉块的 25 页里，转写把图读成
      了文字（26 个块），代价是部分版式信息不再逐字复现——例如第 31 页转写 1222 /
      文本层 1439、第 7 页 2225 / 3245。这是方案二换取「图能被检索到」付出的价钱，
      spec 里以「转写与文本层的对照只呈现不判定」的方式交代，不设比值阈值。

      实测样本（第 10 页增益最大，+2338）：文本层 863 字符只覆盖到该页少量文字，
      转写 3201 字符把整页图表读了出来。

- [x] 3.2 视觉不可用时行为逐字不变。
      验证：把三项 `VLM_*` 之一置空后重跑，断言 `extract_markdown(封思敏.pdf)` 的字符数与 md5
      **与 1.3 记的基线完全相同**。这是本次最重要的一条回归断言。
      **已完成**（脚本 `%TEMP%\vision-verify\t3_2_regression.py`，不调用模型）。三项
      `VLM_*` 全部 pop 掉后：

      ```
      视觉可用 ? False
      缺失项 = ['VLM_BASE_URL', 'VLM_MODEL_ID', 'VLM_API_KEY']

      [✓] 3.2 未配置视觉时，PDF 提取与 1.3 基线逐字相同
            字符数 163545（基线 163545）
            md5    35b7080ded54c3a0bcf3af01e57cdb7f
                   35b7080ded54c3a0bcf3af01e57cdb7f
      [✓] 正文里没有任何视觉标记（`[[` 计数 = 0）
      ```

      字符数与 md5 **都**逐字相同，且正文里一个 `[[` 都没有。数字链条（便于日后
      重新推导基线）：库自己后处理 `172590 -> 172500`，再经本工具
      `normalize_extracted_text` 得 **163545**。

      这条能通过，靠的是两处
      设计上的刻意为之，都是容易在后续改动里被无意破坏的：①`_vision_key_fragment()`
      对非视觉路径（未配置时 PDF 就是非视觉路径）返回 `""`，所以 `_EXTRACT_CACHE`
      的键与改动前逐字节相同；②`_extract_text_with_vision()` 对「未配置 + PDF」返回
      `None` 而不是抛异常，让控制流落回 `_convert_via_library()`。
      （该函数在 5.1 改名为 `_extract_with_vision`，返回的不再是文本而是整个提取结果；
      这两条性质都没变，5.1 后复跑本脚本的 3.2/3.4 断言仍全过。）

- [x] 3.3 图片文件走同一条视觉提取（design.md D10）。
      验证：断言 `ck.extract_markdown(测试上传图片.png)` 含对图内容的文字描述，
      且**不含** `IHDR`、`IDAT`、`pHYs` 这三个子串。
      **已完成**（与 3.1 的真实运行同批，脚本 `t3_1_3_3_real.py`）。实测读数：

      ```
      耗时 10.6s，状态 {'ok': 1, 'empty': 0, 'failed': 0}，499 字符
      [✓] 3.3 正文不含 PNG 块标识（IHDR 0 / IDAT 0 / pHYs 0）
      [✓] 3.3 字符数 499 != 旧噪声基线 89058
      [✓] 3.3 正文含中文（331 个汉字），确实是描述而不是字节

      PNG 视觉块 0 个，文本层 0 字符（图片没有文本层，应为 0）
      ```

      三条断言全过。正文是这张表格图的逐格转写（「分阶段任务目标」「负载规模参数」
      两节，含 2026 下 / 2027 上 / 2027 下三行的时间与人数），**331 个汉字 / 499 字符**，
      对照 1.3 记的旧行为：同一张图 89058 字符的 `IHDR`/`IDAT`/`pHYs` 字节噪声。
      D10 要消掉的就是这个。

      注意 `视觉块 0 个`：一张纯表格图没有可框选的插图，模型直接把它转写成了正文段落，
      于是没有 `[[VISION]]` 标记。**「有视觉块」不是「走了视觉」的判据**——
      判据是走了视觉这条路（提取结果的 `pages` 非空），5.2 的对照表同样按页列出。

- [x] 3.4 视觉未配置时上传图片文件明确失败（design.md D10，spec「视觉提取不可用时明确失败」）。
      验证：置空一项 `VLM_*` 后调用，断言抛 `ChunkingError`，异常消息里说明缺的是哪一项配置，
      且**不返回**二进制解码出来的文本。
      **已完成**（脚本同上）。三项**各自单独**置空、以及三项全缺，共 4 种组合，全部抛
      `ChunkingError` 且消息点名缺失的那一项：

      ```
      [✓] 缺 VLM_BASE_URL 时提取 PNG -> ChunkingError，消息点名 VLM_BASE_URL ? True，未返回文本 ? True
      [✓] 缺 VLM_MODEL_ID 时提取 PNG -> ChunkingError，消息点名 VLM_MODEL_ID ? True，未返回文本 ? True
      [✓] 缺 VLM_API_KEY  时提取 PNG -> ChunkingError，消息点名 VLM_API_KEY  ? True，未返回文本 ? True
      [✓] 三项全缺：ChunkingError
            消息：图片文件需要视觉提取，但视觉配置不全：.env 缺少 VLM_BASE_URL、VLM_MODEL_ID、
                  VLM_API_KEY。补齐后重新预览；本工具不会把图片的二进制内容当作正文。
      ```

      这里 PDF 与图片**故意走不同的分支**：PDF 未配置时静默回退到库提取（3.2 要的就是
      这个），图片未配置时必须明确失败。理由是兜底质量天差地别——PDF 有文本层，
      回退至少拿到可读的字，而图片回退只能拿到 89058 字符的 PNG 块标识噪声，
      且覆盖率还会显示 100%（1.3 的实测），属于静默降级，正是禁区 8 禁止的。

- [x] 3.5 内存提取缓存的键纳入配置指纹（design.md D5）。
      验证：先提取一次并记下文本，改 `VLM_MODEL_ID` 后对同一文件再调一次 `extract_markdown()`，
      断言拿到的是**新**文本（说明没有被 `_EXTRACT_CACHE` 拦下）。改动前这条会失败——
      先确认它在改动前确实失败，再确认改动后通过。
      **已完成**（脚本 `%TEMP%\vision-verify\t3_5_cache_key.py`，2 次真实调用）。
      样本用 1 页的 `测试上传图片.png`。判据用 `vision.extract` 的**调用计数**而不是
      字符数——内存缓存命中时 `vision.extract` 压根不会被调用，这比字符数直接。
      阶段 A 把 `_vision_key_fragment` 打桩成恒返回 `""`，即改动前的三元组键：

      ```
      [✓] 用模型 A(qwen3-vl-235b-a22b-instruct) 第一次提取：499 字符，extract 被调用 1 次
            改 VLM_MODEL_ID -> gpt-4o-mini 后再提取：499 字符，extract 被调用 0 次
      [✓] **改动前确实会踩这个坑**：内存缓存命中（extract 调用 0 次），拿回旧文本 ? True
      ```

      **坑复现了**：换成另一个模型后再提取，`vision.extract` 调用 0 次、拿回的仍是
      499 字符的旧文本。若无此修复，`is_fresh()` 会据此算出不变的 `markdown_hash`
      并报「未变化」，使用者在**毫无提示**的情况下把旧模型的转写入库。

      阶段 B 恢复真键（四元组）、**不清内存缓存**：

      ```
      [✓] 四元组键下缓存**未**命中，确实重新调了 extract（调用 1 次，应为 1）
      [✓] 3.5 拿到的是**新**文本（与模型 A 的不同）? True
            模型 A 499 字符 / 模型 B 546 字符
      [✓] 同一配置再调一次则命中缓存（extract 调用 0 次），文本一致 ? True
      ```

      最后一条是反证：键只是**区分配置**，没有把缓存废掉。

      **顺带量到一件与 D3 模型选型有关的事**：两个模型的输出**结构**不同，不只是措辞。
      模型 A（qwen3-vl-235b）把分阶段目标平铺成普通行，模型 B（gpt-4o-mini）主动
      加了 markdown 标题层级（`## 2026年下半年：`）。本变更的提示词要求的正是
      「输出 markdown 结构（标题层级…）」（2.3），所以换模型会实打实改变切分结果
      ——这既是「换模型必须让缓存失效」这个要求的另一个理由，也说明模型选型
      不是纯粹的质量取舍。

      另：脚本一开始还给 `键片段：非视觉路径 '' / 图片路径 'dae5a1d55f945964'`，
      确认非视觉路径恒为空串——3.2 的逐字不变就建立在这条上。

      **连带回归（后来在 5.3 查出并修掉）**：改键的同时漏改了取键的那一处。
      `cached_quality_report()` 仍按三元组查找，四元组恒不等于三元组，于是质量告警
      在预览里静默消失。本次只写了「改键」，没检查「谁在读这个键」——5.3 的对照
      断言把它抓了出来，修法见那里。

- [x] 3.6 失败页回退到该页文本层，并在状态里显形（design.md D8）。
      验证：伪造「第 5 页 failed、其余 ok」，断言 T′ 的第 5 页正文等于该页文本层，
      且状态计数里 `failed` 为 1。
      **已完成**（脚本 `%TEMP%\vision-verify\t3_6_fallback.py`，假客户端不调用模型）：

      ```
      [✓] 第 5 页 status='failed'，正文 == 该页文本层 ? True
      [✓] 计数 = {'ok': 69, 'empty': 0, 'failed': 1}（failed 应为 1）
      [✓] 清洗后的第 5 页文本层出现在 T′ 里 ? True
      [✓] 未失败的页正文来自视觉转写（第 1 页 'ok'）
      ```

      **顺带量到一件对 D8 的悲观结论有实质修正的事**：失败页的兜底不是「把噪声塞进
      知识库」。既有清洗 `normalize_extracted_text`（`chunking.py:345`）本来就有
      「折叠汉字之间的空格」和「全角拉丁/数字转半角」两类定点改动，实测对文本层
      修复量很大：

      | 页 | 原始 | 清洗后 | 折叠汉字间空格 | 全角转半角 |
      |---|---|---|---|---|
      | 5 | 1734 | 1231 | 503 | 89 |
      | 31 | 1439 | 1065 | 374 | 88 |

      第 5 页 `'ｍ ｗ\r\n摘 要\r\n随 着全球 宽 带 通信 需求 的 快速增 长'` →
      `'m w\r\n摘要\r\n随着全球宽带通信需求的快速增长'`。所以 2.8 记的「文本层被
      空格拆碎」在进知识库前已经被修掉大半，失败页的兜底是可检索的文字。

      但修复面**很窄**，有一条边界要记清楚：`_FULLWIDTH_RANGES`（`chunking.py:306`）
      只含 `０-９` / `Ａ-Ｚ` / `ａ-ｚ` 三个区间，全角标点不在内（D2 的刻意选择，
      不用 NFKC、不碰中文全角标点）。所以第 31 页的 `２．２．２` 只变成 `2．2．2`
      ——**全角句点保留**，`2.2.2` 搜不到，得搜 `2．2．2`。这是设计意图而非缺陷，
      但排查「为什么搜不到小节号」时容易误判成 bug。

## 4. 切分与保护区间

- [x] 4.1 `_protected_regions()`（改动后实际在 `chunking.py:1105`）增加识别视觉块的规则。
      验证：对一个含视觉块的 T′ 调用它，断言返回的某个区间完整覆盖
      从 `[[VISION` 到 `[[/VISION]]` 的整块（含两端标记）。
      **已完成**（脚本 `%TEMP%\vision-verify\t4_protect.py`，**不调用视觉模型**）。
      T′ 只读组装自 `.chunk_cache` 的逐页产物（与 `vision.extract:717` 的拼装、
      `_extract_record:216` 的清洗同路），所以不用再花半小时，也不扰动后台那次真实运行。

      三处实现（都在 `chunking.py`）：
      1. `_iter_region_spans()` **新**——逐行状态机，产出 `(起点, 终点, 类别)`，
         类别 ∈ `{fence, table, vision}`。抽出来是为了让「只认视觉块」的调用方
         复用同一套扫描，而不是各写一份逐行逻辑（两份写法迟早分叉）。
      2. `_protected_regions()` 改为取全部 span，签名与行为对既有调用方不变。
      3. `_vision_regions()` **新**——只取 `vision` 那类，供尺寸兜底用（见 4.2/4.3）。
      标记的写法**引用** `vision.VISION_OPEN_RE` / `VISION_CLOSE_RE` 而不是重写正则：
      标记形态在 2.3 收敛过一次（容忍 `]]` 变体），两处各写一份就会漂移。

      实测（T′ 25925 字符，5 个视觉块）：区间总数 7 = **vision 5 + table 2**；
      每个视觉块都被某个区间**完整覆盖**，且覆盖它的区间与该块**逐字相等**
      （`[28,161)`↔`(28,161)`、`[333,461)`↔`(333,461)`、`[11032,11088)`↔`(11032,11088)`、
      `[24005,24197)`↔`(24005,24197)`、`[25142,25356)`↔`(25142,25356)`）。
      另断言每个块的**两端就是标记本身**——清洗（PUA/全角/汉字间空格）没动过标记。

- [x] 4.2 三种策略下视觉块都不被切开（spec「视觉描述在切分中保持完整」）。
      验证：对同一份 T′ 分别用 `split_paragraph` / `split_markdown` / `split_semantic` 切分，
      断言每一种里，每个视觉块的起止位置都落在同一个 chunk 的 `[start, end)` 内。
      段落与 Markdown 策略靠「块自成一段」满足；语义策略靠 4.1。
      **已完成**（脚本同上）。走的是产品入口 `ck.split_document(text, strat, default_params(strat))`，
      不是直接调三个 `split_*`。三种策略 × 5 个块 = 15 组，**全部通过**：

      | 策略 | chunk 数 | 参数 | 耗时 |
      |---|---|---|---|
      | paragraph | 22 | `chunk_size=800, chunk_overlap=80` | 0.0 s |
      | markdown | 22 | 同上 | 0.0 s |
      | semantic | 81 | `sensitivity=75, min_chars=200, max_chars=1600` | 14.7 s |

      每个块做了**两条**断言：①偏移上落在同一个 chunk 的 `[start, end)` 内；
      ②整块文本作为**子串**出现在某个 chunk 的 `content` 里。第二条是必要的——
      它绕开偏移约定，是使用者真正看得见的那条（①踩过的坑见下）。

      实现：`split_paragraph` / `split_markdown` 走 `_iter_blocks_with_headings` →
      `_scan_lines`，所以那里把视觉块按**围栏同类**处理（`in_vision` 显式跟踪）：
      块内空行不再 flush、块内 `#` 开头不再当标题。`split_semantic` 走 `_iter_atoms` →
      `_protected_regions`，由 4.1 满足。

      **机制层（必须有，否则以上通过没有牙）**：5 个真实块内部**既无空行也无 `#` 行**
      （实测 0/5）。只用真实数据的话，保护逻辑即使完全没写也可能全过。所以构造探针
      （拿真实块改造成带内部空行 / 内部 `#` 行的形态），断言**加保护就不切、去掉保护就切**：

      ```
      [✓] 内部空行：加保护后整块落在一个 block 内，去掉保护就不落在任何一个内
            块划分 3 个（含整块的 1 个） vs 去掉保护 4 个（含整块的 0 个）
      [✓] 内部 `#` 行：加保护后整块落在一个 block 内，去掉保护就不落在任何一个内
            块划分 3 个（含整块的 1 个） vs 去掉保护 4 个（含整块的 0 个）
      ```
      （去掉保护 = 把 `_VISION_OPEN_RE` 打桩成永不匹配，等价于改动前不认识标记。）

      **`_split_oversized` 也必须改，这是本组最要紧的一处**：段落/Markdown 策略下
      「正文 + 视觉块」若同属一个超预算的块，原来的尺寸兜底会**按裸句子**切——
      而一页转写常常就超过块大小（封思敏那份多数如此），于是视觉块被拦腰截断
      且不报任何错。改为按**原子**切（`_iter_atoms_in_range`：句子 + 整体保留区间）。

      **上表的数字会随 `.chunk_cache` 增长而变**：取那次读数时缓存里只有 14/70 页，
      所以 T′ 是 25925 字符、5 个视觉块。后台那次真实运行继续落盘，T′ 会变长、
      块会变多——写满 45 页时复跑本脚本，读数是 T′ 50203 字符、19 个视觉块，
      三种策略 × 19 个块的两类断言**仍然全过**。所以断言的是「每个块都不被切开」，
      不是「恰好 5 个块」；复跑看到数字不同属正常，不是回归。

- [x] 4.3 超长视觉块仍受块大小约束（spec 同名 requirement 的第二条 scenario）。
      验证：构造一个自身超过块大小的视觉块，断言产出多个 chunk、每个都不超过块大小上限，
      且没有把整块塞进一个超限 chunk。
      **已完成**（脚本同上）。构造的块 2286 字符（2286 > semantic 的 1600；
      2082 token > 段落/Markdown 的 800），三种策略都通过：

      ```
      [✓] paragraph：块自身 2082 token > 上限 800 token（前提成立）
            产出 4 个 chunk，每个都不超过上限 —— 最大 800 token / 上限 800 token
      [✓] markdown ：块自身 2082 token > 上限 800 token
            产出 4 个 chunk，最大 800 token / 上限 800 token
      [✓] semantic ：块自身 2286 字符 > 上限 1600 字符
            产出 4 个 chunk，最大 1600 字符 / 上限 1600 字符
      ```
      三者都断言了「没有任何一个 chunk 把整块吞下去」，确认确实在约束下拆了，
      而不是碰巧整块合规。走的是既有 `_hard_split`（单句/单原子仍超预算时的最后手段），
      没有为视觉块另开一条拆分路径。

      **本任务查出一个既有缺陷并修掉了，不属于本次功能但必须修**：
      `split_semantic` 的尺寸兜底是**死代码**。原文是
      `final_units.append(pieces)`，而下面取 chunk 边界用的是 `g[0][0]` 与 `g[-1][1]`
      ——组首到组尾合起来正是拆分前的整个区间，于是切好的片段被丢掉，超限的 chunk
      原样产出。用一段**不含任何视觉块**的长文本（一个原子、必然触发兜底）单独验证：

      ```
      修复前：产出 1 个 chunk，长度 [2030]        最大 2030 字符 vs 上限 1600 -> 超限
              `_split_oversized` 直接把这段切开得到 2 段，长度 [1600, 430]
              但 chunk 只产出了 1 个 -> 切好的片段被丢掉了
      修复后：产出 2 个 chunk，长度 [1600, 430]   最大 1600 字符 vs 上限 1600 -> 未超限
      ```
      所以这个缺陷**与视觉无关**，改动前就在；只是改动前没有任何原子能大到触发它
      （视觉块成为原子之后才第一次够大）。改法是一行：`final_units.extend([p] for p in pieces)`，
      让每段各自成组。**影响面**：语义策略下、组内出现超限原子时，chunk 会变多变小
      ——这是修好，不是回归。6.5 断言的是未启用视觉时的切分一致，走默认 markdown 策略，
      不经这条路径；若日后 6.5 改用语义策略比对，这里的差异是预期的。

      **两处验证脚本自己的坑，记下来免得重犯**：
      1. `vision.iter_vision_blocks` 的 `end` **含闭标记那一行的行尾换行**，而
         `_iter_blocks_with_headings` / `_pack_units` / `_emit` 的 `end` 一律 rstrip 过
         （它们保证 `text[start:end] == content`）。两者恒差一个 `\n`，那不是被切开。
         第一版脚本因此在机制层误报两处失败。修法是统一过 `core()` 收掉尾部换行，
         并补上「整块文本是某 chunk 的 content 子串」这条不依赖偏移约定的断言。
      2. 判据不要放在**最终 chunk 边界**上：那还取决于贪心装填恰好在哪里装满，
         不确定。`_split_oversized` 的保护要判在**原子流**上
         （「有没有原子边界落在块内部」），那才是保护逻辑本身的直接后果。

      **两条本组附带、但为后续任务提前盯住的证据**（都在同一个脚本里，
      不算独立任务）：`_scan_lines` 被重构过，所以要证明围栏行为没被带坏；
      以及未配置视觉时的切分不该受影响。①把改动前的 `_scan_lines` 内联进脚本逐行比对，
      7 个围栏用例（块内空行、``` 里套 ~~~、~~~ 里套 ```、未闭合围栏、代码块里的
      `# 注释`、围栏里出现 `[[VISION`）**输出逐字相同**；第 8 个用例（视觉块里出现 ```）
      **必须不同**，实测确实不同。另断言围栏里的 `[[VISION` 不另开视觉区间
      （区间 `[(0, 33, 'fence')]` 一个）——「谁先开谁吃到自己的闭标记」。
      ②把 `VLM_*` 三项 pop 掉重跑：提取仍是 **163545 字符 / md5
      `35b7080ded54c3a0bcf3af01e57cdb7f`**（3.2 的复查，库那条链仍是 `172590 -> 172500`），
      正文零 `[[`；再把本次新增的两处开关（`_VISION_OPEN_RE`、`_vision_regions`）关掉
      重切，paragraph 与 markdown 各 **83 个 chunk 逐字相同**——改动面确实只落在视觉块上。
      （这只是提前取的一个读数；6.5 仍要按原计划走完整链路端到端验证。）

## 5. 预览呈现

- [x] 5.1 逐页结局计数接进 `preview_chunks()` 的状态串（`11_Q&A_Assistant.py:179`），
      按现有 `viz.summarize_*` 的写法接进 `notes`（spec「视觉解析的结局逐页可查」）。
      验证：伪造「67 成功 / 1 空 / 2 失败」的结果，断言状态串含这三类计数与那 3 页的页码。
      **已完成**（脚本 `%TEMP%\vision-verify\t5_preview.py`，**不调用视觉模型**）。
      走的是**真实** `preview_chunks()`：伪造的只有「本次提取的结果」那份记录
      （按 `ck._extract_cache_key()` 注入内存缓存，于是 `workbench.preview()` 那步是
      缓存命中），切分、覆盖率、渲染、状态串拼接全是产品代码。

      四处实现：
      1. `chunking.py` `ExtractRecord` 增加 `vision` 字段（未走视觉时为 `None`），
         `_extract_text_with_vision` 改名 `_extract_with_vision` 并**返回整个
         `VisionExtraction`** 而不是它的 `.text`——逐页结局只在这一层有，丢掉就再也
         拿不回来。新增 `cached_vision_extraction(path)`，形状与 `cached_quality_report`
         一致（只读缓存、不触发提取、不抛异常）。
      2. `vision.py` `VisionExtraction.summary()` 扩了「未成功」那几行（见下）。
      3. `chunk_viz.py` 新增 `summarize_vision()`。它与 `summarize_quality` /
         `summarize_coverage` 的「合格就返回空串」**故意不同**：那两个是告警，
         这一个全成功时也说——逐类计数是要求交代的内容，不是噪声。
      4. `11_Q&A_Assistant.preview_chunks()` 把 `viz.summarize_vision(vision)` 接进
         `notes`（`quality` 与 `coverage` 之间）。

      实测状态串：

      ```
      ✅ 预览完成：Markdown 结构感知，共 1 个 chunk。确认无误后点击「确认入库」写入知识库。
      视觉解析：70 页 —— 67 成功 / 1 空 / 2 失败
         ⚠️ 未成功 3 处：第 17 页（空）、第 23 页（调用失败）、第 41 页（调用失败）
            · 调用失败的页：正文取自既有文本层，未经视觉处理
            · 空结果的页：模型未返回任何文字，该页的内容未进入提取结果
         ℹ️ 其中 3 页取自落盘缓存，未再次调用模型
      ```

      断言：三类计数都在；**列出的页码恰好是 `{17, 23, 41}`**（用正则把
      `第 (\d+) 页（` 全抓出来比对，不是逐个 `in` 检查）；视觉那一行在 `notes` 里
      而不是替换原来的状态串（断言第一行里没有「视觉解析」）。

      **本任务扩了 2.10 那版 `summary()`，因为照它写会违反 spec。** spec 的 scenario
      是「呈现逐类计数…**并列出这 3 页的页码**」——那 3 页 = 1 空 + 2 失败。
      2.10 那版只把 `failed_pages()` 列进「未成功」，空的那页只出现在计数里、
      页码不出现。改法是新增 `unsuccessful_pages()`（`status != ok`），并把两类
      **分开说明正文来源**：失败页有文本层兜底，空结果页什么也没产出、该页内容根本
      不进 T′（`extract()` 里 `text` 为空串，`extraction.text` 的 join 会把它滤掉）。
      合成一句「这些页的正文取自既有文本层」会把空结果说成有兜底文字，是错的。
      2.10 记的那段示例串因此变长（当时只有一类结局，没暴露这个缺口）。

- [x] 5.2 逐页字符对照接进预览（spec「转写与文本层的对照只呈现不判定」）。
      验证：断言预览里能读到每一页的转写字符数与同页文本层字符数。
      **已完成**（脚本同上）。`chunk_viz.render_vision_block()` 渲染一个默认折叠的
      `<details>` 表格（页 / 结局 / 转写字符 / 文本层字符 / 视觉块 / 说明），
      `render_chunk_report()` 增加 `vision` 形参并在质量区块之后插入；有未成功页时
      默认展开。断言把表格行解析回结构化数据后逐项比对，不是 `in html` 那种弱检查：

      ```
      [✓] 区块里每一页都有一行（实际 70 行）
      [✓] 70 页的页码都能解析出来
      [✓] 以图为主的那一页：转写 1239 / 文本层 0 两个数都在
      [✓] 实测页的两个数照常呈现（转写 1239 / 文本层 1439）
      [✓] 结局列把「空」与「调用失败」分开显示，不是笼统的「失败」
      ```

      表格里的结局措辞与 `summary()` 那一行**逐字对齐**（`失败` → `调用失败`）：
      单独一个「失败」会和「空」混起来，两处说法不一致就会读成两种事实。
      传 `None` 时区块不出现——未启用视觉的预览与改动前逐字相同，脚本里单独断言了。

- [x] 5.3 不以字符数比值告警（design.md D7）。
      验证：构造一页「文本层字符数远低于转写字符数」的用例（第 31 页就是这个形态），
      断言 `viz.summarize_quality` 不因此页给出任何质量告警，两个数仍然照常呈现。
      **已完成**（脚本同上）。

      **先纠正任务原文里的一个读数**：第 31 页实测是转写 1239 / 文本层 1439
      （2.9 的表），文本层**高于**转写，方向与 spec 那个 scenario 相反。符合
      「以图为主、文本层远低于转写」的实测样本是独立图片（3.5：layer 0 /
      转写 499）。所以用例取两页：第 7 页 1239 / **0**（图为主，比值无穷大——
      最容易诱出告警的形态），第 31 页 1239 / 1439（两个数都不小，方向相反）。

      ```
      [✓] 以图为主那一页（1239 vs 0）没有触发任何质量告警
      [✓] 这份文本本身就不含编码噪声，`flagged` 为假
      [✓] 下拉框标签也没有因此出现告警标记
      [✓] 那一页在逐页对照里仍标为「成功」——比值低不改变它的结局
      [✓] **对照**：同样一条取报告的路，换成含私用区噪声的文本就告警
            reasons = ['私用区字符 39.47%（阈值 0.10%）', '全角拉丁与全角数字 59.21%（阈值 0.50%）']
      [✓] **对照**：`summarize_quality` 在这种情况下确实会输出告警（证明它不是恒为空）
      ```

      最后两条**对照**是这条任务的关键：若只断言「不告警」，函数恒返回空串也能通过。
      所以另造一份含私用区与全角噪声的文本走同一条取报告的路，断言它**必须**告警。

      **对照断言当场抓出一个回归，已修——这是本组最要紧的发现**：
      `cached_quality_report()` 取不到任何报告了。3.5 把 `_EXTRACT_CACHE` 的键从
      三元组改成四元组（加了视觉配置指纹），而 `cached_quality_report` 内部仍是
      `_EXTRACT_CACHE.get((路径, mtime, 大小))`——四元组恒不等于三元组，于是返回恒为
      `None`，**导入库的那份质量告警在预览里静默消失**（`summarize_quality(None)`
      恒为空串，不再有告警，也没有任何报错）。用不涉及视觉的 README.md 单独复现：

      ```
      缓存里的键（1 个）：长度 4 -> ('D:\\...\\README.md', 1790146859.3271754, 2897, '')
      改动前查找的键：    长度 3 -> ('D:\\...\\README.md', 1790146859.3271754, 2897)
      这个键在缓存里吗 ? False
      `cached_quality_report(TXT)` 返回：None
      ```

      修法是把键的算法收敛到一处 `_extract_cache_key()`，存与取都调它——两处各写一份
      键，改一处就漏一处，这就是本次的病因。修后同一探针：
      `cached_quality_report(TXT) 返回：报告`。
      （探针脚本 `t5_0_quality_key_probe.py` 保留在同目录。）
      顺带修了 `ExtractRecord` 与 `extract_markdown` 两处已经过期的 docstring
      （一处写「四项一起存」而实际是五项，一处写键只有三项）。

- [x] 5.4 失败页的正文来源在预览里被说明（spec「失败页的正文来源被说明」）。
      验证：伪造一页 `failed`，断言状态串说明该页正文取自文本层。
      **已完成**（脚本同上）。伪造第 23、41 页 `failed`，断言：

      ```
      [✓] 状态串说明调用失败的页正文取自既有文本层
            · 调用失败的页：正文取自既有文本层，未经视觉处理
      [✓] 空结果页的正文来源另行说明（它没有兜底文字，与失败页不同）
            · 空结果的页：模型未返回任何文字，该页的内容未进入提取结果
      [✓] 空结果不是说成「取自文本层」——那是失败页的说法
      [✓] 第 23 页在表格里也写明了正文来源（异常信息跟在来源之后）
            说明列：'正文取自既有文本层；URLError: <urlopen error [SSL]...>'
      [✓] 第 41 页在表格里也写明了正文来源（异常信息跟在来源之后）
      [✓] 空结果页的说明列不写「正文取自既有文本层」
      ```

      面板那一侧本来只打算写异常名，改了：只写 `URLError: …` 会让人以为那一页
      什么都没有，而它其实有文本层兜底。现在说明列以**正文来源开头**，异常跟在后面。
      两个界面（状态串与表格）说的是同一件事，措辞也一致。

      第三条是刻意的：空结果页在 `extract()` 里 `text` 为空串，`.text` 那一层的
      join 会滤掉它，**该页内容根本不进 T′**。把两类并成一句「这些页的正文取自
      文本层」会让使用者以为空的那页有兜底文字可检索，而那页在知识库里什么都没有。

## 6. 端到端验证

- [x] 6.1 全链路入库并可检索（**写 `docs_qualitycheck`**）。
      验证：用封思敏 PDF 预览并入库，随后就「图 2-7」提问，断言检索结果里含描述该图的 chunk。
      收尾：清理该命名空间，断言块数回到 0。
      **已完成**（`t6_1_2_3.py`，日志 `t6_1_2_3.log`，PASS）。T′ = 78568 字符 /
      md5 `9286efb26939b6b0e96c1540c4a53b30`，26 个视觉块，61 个 chunk，失败页 0。
      预览不写库、入库 61 块、清理 61 块后回到 0，全部成立。

      **这条第一次是红的，原因不在产品而在判据。** 第一版判据是「在 `run()` 返回的
      字符串里找该视觉块正文的一个 40 字符窗口」。实测该块落在 chunk #31
      `[37576, 38926)`（1350 字符），视觉块正文从**第 1105 字符**起，前面全是表 2-2；
      而库的 `_search` 把每条结果渲染成 `meta["content"][:200] + "..."`
      （`tools/builtin/rag_tool.py:406`，形参 `max_chars` 在这条路径上根本没被用到）。
      **所以无论排名第几，那个窗口都不会出现在返回串里——判据结构性不可能成立。**

      改判据前先量了真实排名（`t6_1_rank2.py`）：

      | 路径 | 目标块排名 | 分数 |
      |---|---|---|
      | 裸向量检索（无改写），61 块全排 | **第 1 / 61** | 0.6322 |
      | 高级检索 MQE+HyDE（库的默认，应用走的就是这条） | **第 2 / 20** | 0.7060 |
      | `RAGTool.run(action="search")` limit=5 | 第 2 条就是它 | 0.708 |

      改成比对**检索到的 chunk 原文**（与 `_search` 内部同一条 `search_advanced` 调用）
      之后通过：排第 2 / 5，五条分数 0.708 / 0.694 / 0.678 / 0.676 / 0.664。
      两条改写路径都把它排进前二，所以这不是「勉强够到 top-5」。

      **顺带记下一条与本次变更无关、但真实存在的可见性缺陷**：即便它排第 2，用户在
      检索结果里看到的也只是表 2-2 的表头——图 2-7 的描述一个字都露不出来。原因是
      视觉块被并进了同一页那张表所在的 chunk 的**尾部**，而这块 1350 字符又超出了
      `chunk_size=800`（markdown 策略下表格是原子的，切不开）。这是切分策略与
      「缝法 B」叠加的后果，不在本次变更范围内，另议。

- [x] 6.2 预览与入库是同一份切分（主 spec「预览与入库结果一致」）。
      验证：入库后回读该文档的 chunk 数、每个 chunk 的内容与顺序，断言与预览展示的完全一致。
      **已完成**（同上脚本，PASS）。库里 61 块 == 预览 61 条；回读的内容与顺序与预览
      **逐字相同**；`[start, end)` 多重集逐条相同；回读的 `start` 递增；
      `split_strategy == ['markdown']`；`split_params == {'chunk_size': 800, 'chunk_overlap': 80}`。

      **踩过两次的坑，记在这里**：`commit_preview()` → `workbench.commit()` 会把预览
      快照**消费掉**，之后再读 `wb.materialized` / `wb.chunks` 都是空的。凡是「入库后
      与预览比对」的断言，快照必须在入库**之前**取。第一次栽在 `chunk_index`
      （`_emit` 根本不产出这个键），第二次栽在这里；现在脚本里另有一条断言把
      「入库后快照已空」这件事也钉住了，免得再犯。

- [x] 6.3 覆盖率与偏移仍正确（主 spec「切分不丢内容」与「入库 chunk 保持可检索」）。
      验证：断言预览的覆盖率等于 chunk 位置区间并集 ÷ `len(T′)`（不是字符数之和 ÷ 长度），
      且随机抽 3 个 chunk，用其 `[start, end)` 切 T′ 得到的字符串与 chunk 文本相同。
      **已完成**（同上脚本，PASS）。实测：

      ```json
      {"text_len": 78568, "chunk_count": 61, "total_chunk_chars": 81876,
       "covered_chars": 78504, "coverage_ratio": 0.9991854190001018,
       "redundancy_ratio": 0.042953225313359766,
       "position_available": true, "below_threshold": false}
      ```

      `covered_chars` 78504 == 位置区间并集 78504（分母是并集，不是 Σchunk）；
      `coverage_ratio` == 并集 ÷ `len(T′)`；Σchunk 81876 ≥ 并集 78504，多出的 3372
      就是重叠（冗余 0.0430）。随机抽的 3 个 chunk（种子 20260924）区间切回 T′
      与内容逐字相同：`[0, 1295)` 答辩委员会名单 / `[61251, 62534)` 3.3.2.4 训练策略 /
      `[74058, 78568)` 参考文献。

- [x] 6.4 参数变化后预览失效仍成立（主 spec「参数变化后预览失效」，design.md D5 的要害）。
      验证：先预览，然后改 `VLM_MODEL_ID`，不重新预览直接点入库，断言被拒并要求重新预览。
      补充：若这条通过不了，说明 3.5 没做对，回头修 3.5。
      **已完成**（`t6_4.py`，PASS）。用 **1 页 PNG** 而不是 70 页 PDF——换模型后必然要
      真的重提取一次（缓存键变了，不可能命中），PDF 就是 70 次调用，PNG 只要 1 次，
      证据强度一样。三条按顺序全部成立：

      1. A = `qwen3-vl-235b-a22b-instruct`（配置指纹 `dae5a1d55f945964`）预览后
         `is_fresh` 为真，且库仍为空；
      2. 改成 B = `gpt-4o-mini`（指纹不同）后 `is_fresh` 为假，**并且
         `vision.extract` 确实被再调了一次**（调用记录 `('测试上传图片.png', 'gpt-4o-mini')`）
         ——否则「被拒」可能只是空状态蒙的；入库返回拒绝串，库里 0 块；
      3. **对照组**：换回 A 之后 `is_fresh` 恢复为真、入库成功（1 块）——证明第 2 步的
         拒绝是这次配置改动引起的，不是别的原因。

      **非空性另外查了落盘缓存**：`.chunk_cache/fac9f187ce89a281/` 下两个配置各有一份
      `page-0001.json`，`status` 都是 `ok`，文本 499 字符（A）vs 541 字符（B），措辞也不同。
      失败页是不落盘的，所以文件存在本身就证明 B 的调用成功、且产出的确实是不一样的东西。

      拒绝串原文是「❌ 尚未预览，或预览之后参数/文件已改动。请先点击「预览切分」，
      确认结果后再入库。」——同一句要覆盖「从未预览」这种情形，所以说的是
      「请先点击『预览切分』」而不是主 spec 字面的「重新预览」。断言查的是实质
      （串里给出了重新预览的动作），不是措辞。

- [x] 6.5 未配置视觉时的全链路回归。
      验证：置空一项 `VLM_*`，对封思敏 PDF 走完预览与入库（`docs_qualitycheck`），
      断言提取文本与 1.3 的基线逐字相同、切分结果与本次改动前的同参数结果一致。
      收尾：清理该命名空间，断言回到 0 块。
      **已完成**（`t6_5.py`，PASS）。比对基准是 `git show HEAD:chunking.py` 真的加载出来的
      那个模块（HEAD `ad5c94e` 就是改动前的提交，实测它**不含** `import vision`），
      不是凭记忆写的期望值。

      实测读数：**整条路径 0 次模型调用**（挂了计数器）；提取 163545 字符 /
      md5 `35b7080ded54c3a0bcf3af01e57cdb7f`（与 1.3 基线逐字相同）；正文 `[[` 计数 0；
      `cached_vision_extraction` 为 `None`；库链仍是 `172590 -> 172500`；
      `split_document` 与改动前**逐字段相同**（83 个 chunk）、diagnostics 相同；
      `materialize` 与改动前逐字段相同（83 条）；预览 HTML 与状态串里都没有「视觉」；
      入库 83 块 == 预览 83 条；回读内容与改动前的物化结果逐字相同；清理 83 块后回到 0。
      （83 块 vs 走视觉时的 61 块——未配置时文本是 163545 字符而不是 78568。）本条**最初是红的，两个原因都在脚本里**：
      (a) 置空 `VLM_MODEL_ID` 发生在 `import 11_Q&A_Assistant` **之前**，而该模块在
      **模块级**执行 `load_dotenv(override=True)`（`11_Q&A_Assistant.py:14`），导入时
      把变量从 `.env` 又读了回来——于是第一次跑实际上测的是**走了视觉**的那条路
      （78568 字符），7 条断言集体变红且互相矛盾。现已把置空移到导入之后，并加了一条
      前置断言（`missing_config_names() == [blanked]` 且 `load_config() is None`），
      置空无效就直接停，不再产出空判据。
      (b) 在 (a) 未修时，`head.split_document(text, ...)` 喂进去的是**带视觉标记的**文本，
      而 HEAD 的切分器不认识视觉块、把块从中间切开，于是「改动前」那一侧出现了孤立的
      `[[/VISION]]`——那次比对本身没有意义。修好 (a) 之后两侧吃的都是无标记的
      163545 字符文本，比对才成立。

- [x] 6.6 存量数据未被触碰。
      验证：重跑 1.2 的脚本，断言 `docs_yyh` 与 `default` 的份数、块数、逐份明细
      **与 1.2 记下的读数完全相同**。两者都是只读的。
      **已完成**（`t6_6.py`，PASS）。用 `RAGTool(rag_namespace=...)` + `ck.get_store` +
      `ck.list_documents` 读回，按 basename 前缀匹配逐份核对，缺一份或多一份都会被点名。
      结果与 1.2 一字不差：`docs_yyh` 5 份 / 219 块（91 / 42 / 40 / 25 / 21）、
      `default` 4 份 / 100 块（78 / 19 / 2 / 1）、`docs_qualitycheck` 0 / 0。
      三个命名空间在全部 6.x 跑完之后又独立复核了一次，仍是这三个数——测试都在自己的
      命名空间里读写并在 `finally` 里清干净。

- [x] 6.7 密钥未泄漏（禁区 7）。
      验证：`grep -rn "VLM_API_KEY" --include=*.py .` 只出现在读取配置的那一处，
      没有任何地方把它打印、写进 artifact 或写进日志。
      **已完成**（`t6_7.py`，PASS）。分「名字」与「值」两条独立检查，缺一不可，
      且**全程不回显密钥**（只打印文件名、行号与计数）。

      - 名字：`VLM_API_KEY` 只出现在 `vision.py`（第 68、163 行），其中真正读环境的
        只有一处；命中的行里没有任何一行把它交给 `print` / 日志 / 异常输出；
        全工作树也没有 `print(...api_key...)` 这类形态。
      - 值：真值不存在于 54 个被 git 跟踪的文件、工作树里除 `.env` 之外的 136 个文件、
        以及本次变更的 6 个 artifact 中的任何一个；它确实在 `.env` 里，而 `.env` 被
        `.gitignore:4` 忽略（`git check-ignore -v .env` 确认）；`.env.example` 里没有真值
        （历史坑，这里守着）。

      **第一次跑是假通过**：查名字时用的是 `git ls-files`，而本次新增的 `vision.py`
      **还没被提交**，`git ls-files` 看不见它，于是「一个都没出现」。改成遍历工作树
      （跳过 `.git` / `__pycache__` / `.chunk_cache` 等）之后才查得实。
